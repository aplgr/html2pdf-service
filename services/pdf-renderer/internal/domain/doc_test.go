package domain

import "testing"

func TestDomainPackage_IsPresent(t *testing.T) {
	// This package currently serves as the domain boundary marker.
	// Keep a minimal test so `go test ./...` includes this module path.
}
